<h1 class="page-header"> Create a new Category!</h1>

  <%= form_for [:admin, @category] do |form| %>

  <div class="form-group row">
    <div class="col-md-4">
      <%= form.label :name %>
      <%= form.text_field :name, class:"form-control" %>
    </div>
  </div>

  <% end %>
