
<% if current_user == nil %>
  <div class="row">
    <div class="jumbotron review col-md-4">
      <div>
        <b>User Name:</b>
        <%= review.user.first_name %>
      </div>
      <div>
        <b>Rating: </b>
        <%= review.rating %>
        / 5
      </div>
      <div>
        <b>Review:</b>
        <%= review.description %>
      </div>
    </div>
  </div>

<% else %>
  <div class = "row">
    <div class="jumbotron review col-md-4">
      <div>
        <b>User Name:</b>
        <%= review.user.first_name %>
      </div>
      <div>
        <b>Rating: </b>
        <%= review.rating %>
        / 5
      </div>
      <div>
        <b>Review:</b>
        <%= review.description %>
      </div>
      <% if current_user.email == review.user.email %>
        <%= link_to fa_icon('trash'), product_review_path(@product, review), class: 'btn btn-sm btn-danger del-btn', method: :delete%>
      <% end %>
    </div>
  </div>
<% end %>
